# Intelligent Freight Parser

An automated data extraction pipeline built with Python. It utilizes `pdfplumber` and the OpenAI API (GPT-4o-mini) to extract structured financial data from logistics invoices and compile it into a CSV database.

## Features
- Automated PDF text extraction
- Pydantic validation for structured AI output
- Duplicate invoice detection
- Built-in logging and error handling
- Summary reporting generation